export type IColors = {
  primary: string;
  background: string;
  overlay: string;
  card: string;
  text: string;
  secondary: string;
};

export interface AppTheme {
  light: IColors;
  // dark: IColors;
}
